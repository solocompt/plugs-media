============
Installation
============

At the command line::

    $ easy_install plugs-media

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv plugs-media
    $ pip install plugs-media
